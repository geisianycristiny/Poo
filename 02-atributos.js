class Pessoa {
    constructor(nome, idade, atleta, peso) { // atributos 
        // Inicialização dos atributos
        this.nome =nome;
        this.idade = idade;
        this.altura = this.altura;
        this.peso = peso;
    }
}